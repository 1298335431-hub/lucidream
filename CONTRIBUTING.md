# 开发指南

先按 [README](README.md) 使用 mock 模式启动，避免开发验证消耗真实额度。保持前端、Python 后端和 Node OAuth 适配服务的职责边界，提交中不得包含实际密钥、体验码和用户数据。

## 提交约定

- 功能：`feat: ...`
- 修复：`fix: ...`
- 文档：`docs: ...`
- 测试与工程维护：`test: ...` / `chore: ...`

提交前运行 `pnpm typecheck`、`pnpm build`、`pnpm test:oauth` 以及 README 中的后端测试命令。界面改动额外检查桌面与手机宽度，确保下载结果与预览一致。

## 浏览器回归

`tests/browser-*.mjs` 保留开发阶段的独立回归脚本，覆盖分享、账号菜单、页面布局和动画等场景。部分脚本使用开发机的 Playwright 绝对路径，不是开箱即用的 CI 测试，运行前需将导入路径替换为本机 Playwright 安装路径，并查看脚本要求的测试地址及前置状态。

不要把真实用户 Cookie、OAuth 令牌或线上账号内容放入测试 fixture。真实登录必须由账号持有人完成；任何实际发布操作必须再次由用户确认。

## 知识资料

自建知识卡保留在 `data/knowledge/自建知识库/`，包括审校状态；不应将草稿误标为已审核。下载的书籍、OCR 原文及生成的索引数据库不入库。正式使用前检查 `docs/知识库商用权利台账.md`。

## 文档维护

README 描述当前可用能力，阶段审计文档保留历史。修改权限、存储或分享机制时同步更新 README、架构说明和安全说明，避免把规划中的功能描述为已经实现。
