# V1 上线前验证报告

日期：2026-09-14。结论：现有基础测试通过，但尚不满足直接上线条件。

## 2026-09-15 修复复验补充

下文三个明确代码问题均已修复：新建梦境不删除历史；解读 processing 超过 15 分钟后转为可手动重试的中断状态（GET 不触发模型），通过启动时间条件保护新任务不被旧请求覆盖；三处 replaceAll 改为 ES2020 兼容写法。恢复页面可识别中断状态并保留记录供重新解析。

复验：后端 177 项、前端单元 15 项、浏览器 12 项全部通过，默认 TypeScript 检查与生产构建通过。浏览器审计的误删复现断言已改为禁止删除，另增加中断恢复页面断言。未调用真实模型，未消耗生图额度。依赖安全、正式环境与备份恢复等其他待验收项仍然有效，不能因此认定已经满足全部上线条件。

本轮为检查与问题复现，没有修改业务逻辑，没有调用真实文字模型或生图接口，没有消耗邀请码额度。新增 `tests/browser-release-audit.mjs` 作为浏览器检查脚本。首页展示用旧套餐文案按用户要求不在本轮修改范围；当前 V1 生图美术效果按用户已接受的标准验收。

## 上线前需处理的明确代码问题

### 1. 解读页“记录另一个梦”会删除上一条记录（高优先级）

- `src/App.tsx` 的该按钮仍调用 `startOver`，确认后发送 DELETE 请求，而非仅开启新梦境。
- 浏览器拦截测试已复现删除请求；未删除真实用户数据。
- 梦境册“记录新的梦境”使用的是不删除历史的路径，两处行为不一致。
- 建议：新建梦境与删除记录使用不同操作；新建只清理当前编辑状态，删除需明确提示删除对象。

### 2. 文字解读在服务重启后可能永久卡住（高优先级）

- 在临时数据库建立 processing 状态的解读任务，然后重建应用模拟重启。
- 查询解读、显式 retry 均返回 409；即使任务时间已非常久远，也不会恢复。
- `backend/app/main.py` 对 processing 直接拒绝，没有过期任务恢复机制。图片任务已有恢复逻辑，不能覆盖文字解读。
- 建议：为文字任务增加超时、任务归属或租约与可恢复状态，避免重复执行，同时允许真正中断的任务重新启动。

### 3. 项目默认类型检查失败（中优先级）

- `pnpm exec tsc --noEmit` 报 3 处 replaceAll 类型错误。
- 涉及 `src/App.tsx` 两处、`src/components/ui/dream-date-picker.tsx` 一处；tsconfig 的 lib 仍为 ES2020。
- Vite 构建通过不代表类型检查通过。此前使用 ES2021 命令行覆盖参数的检查不能代表默认配置可用。
- 建议：明确浏览器支持范围，统一代码与目标配置，并将默认类型检查纳入发布验证。

## 已执行的检查

| 检查 | 结果与边界 |
| --- | --- |
| 后端 pytest | 175 通过；281 条警告，不等于没有运行风险 |
| 前端单元测试 | 15 通过 |
| 既有恢复与梦境册浏览器测试 | 8 通过 |
| 新增浏览器检查 | 桌面和 390px 宽度导出测试通过；另 1 项成功复现删除问题，不能算业务验收通过 |
| 前端生产构建 | 通过；主 JS 约 600KB，有包体积警告 |
| 默认 TypeScript 检查 | 失败，见上述问题 3 |
| Python 语法解析 | 46 个 Python 文件通过 |
| Python 依赖一致性 | pip check 通过 |
| 本地 OCR 实际引擎 | 使用内存合成图片执行 RapidOCR 检查成功；并非识别准确率评测 |
| 当前 SQLite 完整性 | integrity_check 返回 ok；不是持久化备份验收 |
| 运行中服务 | 前端首页 200；未登录请求历史和图片均 401 |
| 模拟生产认证 | HTTPS TestClient 下 Secure、HttpOnly、SameSite=Lax Cookie、API no-store、错误 Origin 403、退出后失效通过 |
| 构建产物检查 | 未发现当前配置的模型密钥；未发现 .env、数据库、source map 文件，不等于完整秘密扫描 |

浏览器导出检查使用隔离浏览器与模拟接口，屏蔽外部网络。验证 PNG 为 1080×1440，日期及标识开关刷新后保留，无横向溢出、无页面脚本异常，也未触发生图或删除请求。字体使用可用的本地回退，不代表所有设备视觉一致；尚未覆盖真实 iOS Safari。

## 依赖安全与正式环境缺口

- pnpm audit --prod：当前运行依赖未报告漏洞。
- 完整 pnpm audit：9 项告警（6 high、3 moderate），涉及 Vite、PostCSS、nanoid 等开发/构建依赖。需要升级并回归；不能直接等同于 9 个可被线上利用的漏洞，也不应将 Vite 开发或预览服务直接作为正式部署方案。
- 对实际安装的 66 个 Python 包进行 OSV 查询，Starlette、pip、pytest、setuptools 的版本命中安全公告；Starlette 是后端运行依赖，应优先评估修复。公告可能有别名，未重复计数，也未逐项验证可利用性。
- 依赖安全查询是当时结果，不保证以后没有新公告；本轮没有执行依赖升级。
- 当前运行配置仍为 development，CORS 仅本地地址。正式域名、HTTPS、生产配置、反向代理、持久化磁盘与长期备份恢复还没有完成正式环境验收。
- 当前 moderation_mode 为 disabled，需要明确正式发布时的内容处理方案。本轮未擅自开启付费审核。
- 尚未做真实线上并发、长时间运行、实际供应商超时及限额验证。模拟接口成功不能证明真实生图服务在上线环境稳定。
- `backend/IMAGE_RECOVERY.md`、`backend/INVITE_ACCESS.md` 部分描述与现有质量重绘、额度及真实历史逻辑不一致，需同步维护，避免按旧说明操作。

## 建议处理顺序

1. 修复误删历史、文字任务中断恢复、默认类型检查。
2. 处理依赖安全告警并重跑现有测试；补充修复后的回归断言。新增审计脚本中目前用于复现删除请求的断言必须改为禁止删除。
3. 在正式部署环境验证 HTTPS、登录、账号隔离、五次额度、持久化与备份恢复。
4. 经确认后执行一轮真实模型端到端验收，再开放邀请码。

本报告覆盖已执行的自动化检查及重点边界复现，不代表所有代码路径、所有设备或供应商故障均已穷尽验证。
