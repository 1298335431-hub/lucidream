# 异常色带修复

原始 960×1280 图片右侧约 27% 为近白色区域，说明文件比例正确，异常已存在于上游底图。网页遮罩将其显示成灰色。

- 构图规则移除了主体偏左、自然负空间、低视觉密度等版式引导。保留既有统一画风和 3:4 比例。
- 本地质检在 OCR 基础上检查宽度占比至少 6% 的低纹理条带，并要求存在贯穿画面的强分界；同时检测整张近纯色图。
- 横向、纵向均检查。正常平滑渐变不因低纹理单独被拒绝。
- 文字与色带共用一次自动重试预算，按拒绝原因补充重试指令。再次失败返回 failed/contains_band，不提供图片下载地址。
- 已确认异常旧图单独标记为不合格，保留记录；后续由用户手动生成。

验证：本次真实原图被拦截；109 项后端测试通过；前端构建通过。测试包括四向白/灰/彩色色带、正常雾气渐变、连续失败停止重试与下载拦截。未调用付费生图。

限制：这是针对明显平坦色带的启发式检查，不保证识别所有构图异常；带复杂纹理的分栏可能漏检，规则需依据后续真实样本继续校准。
