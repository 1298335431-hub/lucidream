# 梦卡项目状态与交接

> 最后更新：2026-09-03。新开 Codex 任务时，先读取本文件、`docs/第二阶段技术开发文档.md`、`docs/梦卡视觉与出卡规范.md`、`data/knowledge/README.md`、`docs/技术适配声明.md` 与当前 Git 状态。

## 已确认的产品决定

- 产品名称为梦卡。
- 当前只做桌面 Web 端；卡片下载规格为 1080 × 1440 PNG，适合手机保存和分享。
- 第一阶段只覆盖梦境输入、浏览器语音入口、梦象提取、人工修订确认与会话恢复。
- 阿里云体系作为模型供应商；第一阶段实际模型是通义千问 Qwen3.8-Flash。
- 2026-09-01用户已同意进入第二阶段并核查/下载RAG素材，覆盖此前“不要开始素材准备”的暂停指令。已下载两个古籍扫描候选；未确认生产商用白名单，暂不进行全量OCR、向量化或正式解读。
- UI 视觉已确认并冻结。任何未来视觉改动必须遵循用户指定的 Lucidream 模板与 Product Design 约束。
- 2026-09-03 已确认产品唯一 Logo 为星光、月亮前卡与后置倾斜卡的组合。网页页头使用小尺寸一次性动态版，导出梦卡使用同图形的静态低饱和版；完整约束见 `docs/梦卡视觉与出卡规范.md`。

## 背景视觉更新（2026-08-31）

- 用户明确要求替换最底层背景，复用 `/Users/a7/Documents/Codex/2026-08-31/new-chat/components/ui/aurora-background.tsx` 和对应 Tailwind 动画配置的极光效果。
- 本项目适配为 `src/components/ui/aurora-background.tsx` 及同名 CSS；保留原渐变、蒙版、模糊、混合效果和 60 秒循环，使用 Tailwind 4 的 `@theme` 注册动画，颜色变量局部作用于背景。
- 背景固定铺满视口，`pointer-events: none`、`aria-hidden`，与 App 平级置于根容器底层。不复用参考组件中的布局包装或新增 main，避免改变任何前景布局、内容与交互。减少动态效果偏好下显示静态极光。
- 用户确认保留极光。排查到的三个接近 100% CPU 的进程实际为「洞察领航」Next 工作进程，已退出；不要据此归因于极光。现已恢复背景，并通过 Page Visibility 在页面隐藏时暂停、可见时继续。前台动画仍为原版，尚未做渲染性能量化，不承诺设备不发热。
- 构建及 TypeScript 检查通过；桌面关键元素位置尺寸与替换前一致，按钮和输入检查通过。截图在 `work/aurora-desktop.png`、`work/aurora-mobile.png`。

## 第一阶段当前状态

- 用户因卡顿和 CPU 负担要求彻底移除灰色鼠标点击粒子效果；其组件文件、入口导入和挂载均已删除。不要重新加入该效果。极光背景、语音与原有交互保留。

- 后端：FastAPI、Pydantic、SQLite、固定状态机和 `/api/v1` 接口已实现。
- 前端：React/Vite 页面已真实调用后端，支持输入、梦象编辑、单轮澄清、确认与刷新恢复。
- 测试：pytest 30 项通过；生产构建通过；一条真实 Qwen3.8-Flash 测试链路已跑通且测试数据已删除。
- 待产品经理操作确认：浏览器麦克风允许、拒绝和不可用时的语音入口表现。

### 2026-08-31 语音与输入交互修订

- 轮换提示仍为 7 秒一轮、逐字间隔 90ms；文字编辑或语音连接、收听、收尾期间隐藏并暂停轮换。
- 输入区保持 28px 单行起始高度，随实际文字（含临时转写）换行增高，最高 184px 后内部滚动；点击或聚焦不会单独撑大输入区。
- 语音显示临时转写，只将新增最终结果追加到正式草稿，避免累计结果重复。手动编辑接管当前可见文字并取消旧语音会话，防止迟到结果覆盖。
- 收听/收尾时禁止提交，等待最终文字后再确认；示例按钮暂时不可用，避免混入示例内容。
- 权限、网络、设备、无声音、语言不支持、超时等异常以输入框下方浅灰提示区呈现，延续 Lucidream 字体、灰阶、圆角与轻量淡入；提供“继续文字输入”。
- 验证：生产构建与 TypeScript 检查通过；`node --test tests/speech.test.ts` 的 8 项测试通过；桌面与 390px 窄屏检查通过。模拟语音入口仅在 `work/speech-ui-check.html`，不进入生产入口，也不调用麦克风。
- 本次没有真实录音或调用云端语音服务。Chrome 的真实语音端到端、系统授权仍需用户实测，不能由模拟测试宣称已通过。

## 运行方式

- 后端：`PYTHONPATH=backend .venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8001`
- 前端：`pnpm dev --host 127.0.0.1 --port 5173`
- 前端 API：`http://127.0.0.1:8001/api/v1`
- 不要停止占用 8000 的其他本地项目。

## 明确暂缓与边界

- 第二阶段已开始审核适配和书籍候选素材准备；云审核真实联调待服务/凭证/地域确认，RAG入库待版本、使用条件与OCR质量确认。图片、卡片下载、梦境册、登录、云同步和部署仍未启动。
- 不读取、打印、提交或在聊天中索要 API Key；真实 Key 仅保存在本地 `.env`。
- 不把本地关键词危机中断描述成阿里云内容安全审核。

## 下一步

### 2026-09-01 第二阶段启动（最新）

- 已增加内容审核SDK适配、配置检查接口、显式真实冒烟命令，以及梦象输入/输出/人工修订的条件式审核关口。46项后端测试通过。默认disabled；本地无审核凭证和地域，未调用云端、未改真实.env、未重启已有后端。不得将本轮描述成“真实审核跑通”。
- `data/knowledge/解梦素材/` 统一保存解梦候选原始素材：已下载《梦林玄解》1213页PDF和《梦占逸旨》77页PDF，并加入两份 Project Gutenberg 英文 UTF-8 文本；每份均保留来源、许可提示和SHA-256。少量样页查看及文件校验不代表完整性、OCR或商用验收，仍是候选而非生产书库。
- 两份英文 TXT 已离线清洗与结构化：弗洛伊德正文第 I–VII 章生成540片段，Miller 2256词条生成2265片段；236个仅交叉引用的片段默认禁止检索。输出保留原文件行号、SHA-256和来源 URL，抽样同时匹配清洗正文与原始行范围。
- 已真实调用阿里云百炼 `text-embedding-v4`，以 512 维建立 `data/knowledge/dream_sources.sqlite3` 本地候选索引，共 2,569 条，首次构建计数 387,753 tokens。索引可续跑、保留原文/行号/来源/权利状态，且未入 Git。所有片段仍是未商用批准候选素材，不得写成生产 RAG 已完成。
- 已完成通义千问字面英文梦象查询扩展、关键词+多查询向量混合召回和 `qwen3-rerank` 重排。查询扩展只发送用户确认的结构化梦象，不重复发送原始梦境。五组真实验收的鹿、森林、水、旧房子等命中显著改善；“被陌生人追赶”在书中无独立同名词条，系统返回相关 Anxiety、Escape 及具体追跑原文，未伪造条目。下一步是将前 5 条可追溯来源接入正式解读草稿与引用校验。审核产品/地域和两本 PDF 的权利/OCR 仍另行待定。
- 已完成正式文字解读 API：`POST/GET /api/v1/dreams/{session_id}/interpretation`。`Qwen3.7-Plus` 只能使用前5条实际检索原文，后端强制验证 source ID 与连续原文 quote。同修订成功结果幂等落盘；失败不自动重试，只接受用户显式 `retry=true`。数据库结构版本2，69项后端测试通过。
- 五组合成梦境真实端到端联调：森林与鹿、水中行走、旧房子、被陌生人追赶首轮完成，用时15.295–17.745秒，均有3条严格引文、5个来源与恢复一致性。坠落首次契约失败被阻止，独立明确诊断调用成功。首轮小样本80%未达90%目标，不宣称已达产品验收线。
- 当前阻塞生产开放的两项不变：候选书籍尚未商用批准，云端输出内容安全审核尚未真实启用。所以 API 明确返回 `production_eligible=false`、`review_status=not_enabled`，暂不接入用户前端的正式发布路径。
- 2026-09-01 已新增 `docs/知识库商用权利台账.md`。首发地域暂按中国大陆，并记录美国来源状态；两本已索引英文资料均整理到 `evidence_ready_for_legal_review`，但这只是“证据可提交审核”，不是批准。manifest、片段和 API 的 `commercial_approval=false` / `production_eligible=false` 均未改。下一步由业务责任人与法律/版权审核人签字后，才能建立机器可读生产白名单和重建索引。
- 2026-09-01 已运行 20 组**合成梦境**的完整 RAG→解读验收。临时数据库、生产白名单不变、云审核仍 disabled。首次完整通过 12/20（60%），通过样本平均 15.943 秒、最长 17.402 秒，均有来源/连续引文/恢复一致性；8 组在生成阶段安全失败并未自动重试。结果位于 `work/rag-20case-evaluation.json`，详见 `docs/第二阶段技术开发文档.md`。此结论低于 90% 成功率门槛，不能开放生产；后续需在用户明确同意额外模型费用后，用新增失败码记录做定向诊断与修复，再重新评估。
- 2026-09-01 已完成《梦占逸旨》77页扫描件的目视页范围核验，未做OCR或模型调用。封面、1939出版信息、自序、目录、空白页和标注“原书缺页”的 PDF 27–28、33–36 均排除；首批仅建议内部 OCR 小样 PDF 13–26 页（卷二、卷三连续正文，页内目录列仍需排除）。详见 `data/knowledge/解梦素材/mengzhan-yizhi-1939/page-range-audit.md`。权利状态、生产白名单与索引均未变更。
- 2026-09-01 已在《梦占逸旨》PDF 13–26 页运行本地 OCR 小样。macOS Vision 与新增的 `rapidocr-onnxruntime==1.4.4` 都未达可入库质量：竖排列序、版心/目录干扰、异体字和常用字误识别明显。RapidOCR 原稿仅作内部证据保存于 `data/knowledge/解梦素材/mengzhan-yizhi-1939/ocr-sample/raw-rapidocr.jsonl`，未生成简体文本、未切分/向量化、未改变生产白名单。下一步必须选择专业/云端竖排古籍 OCR 或人工转写校对，先重跑同一14页再评估。
- 2026-09-01 用户确认生产知识库改为自建现代简体中文路线。已新增 `data/knowledge/自建知识库/知识卡规范.md` 和 `首批50个梦象目录.md`，定义文化依据、现代研究和梦卡原创三层结构，并强制记录来源、权利、撰写、审校与版本状态。当前仅完成目录与规范，未写最终解梦正文、未向量化、未调用模型。英文书和古籍扫描件保留为内部研究/对照候选，不再作为默认生产正文来源。下一步是在用户确认目录后，为 5 张代表性知识卡先核验依据、再制作人工样稿。
- 2026-09-01 用户确认先试做 5 张。已新增 `data/knowledge/自建知识库/首批5张知识卡依据清单.md`，完成被追赶、迷路、水域、已故亲友、牙齿脱落的首轮公开资料检索。牙齿脱落有直接但初步的实证研究，被追赶有小样本相关研究，已故亲友有系统综述；迷路和水域主要只有主题分类，不能形成固定象征结论。所有论文仅作引用与原创概括，全文不进入生产 RAG；古籍网页只作本地扫描定位线索。尚未写正式样稿、未向量化、未调用模型。下一步先确认是否接受这一证据尺度，再人工撰写 5 张样稿。
- 2026-09-01 已按依据清单完成 5 张现代简体中文样稿，位置为 `data/knowledge/自建知识库/样稿/`。样稿覆盖被追赶、迷路、水域、已故亲友和牙齿脱落，均标记 `draft`、`original_pending_review` 和“AI辅助初稿”，没有固定象征、吉凶、预言或诊断结论。传统原典未完成核验的部分保持空缺或候选定位，不让模型补造来源。尚未完成人工内容审校、权利签字、向量化或生产入库，本轮未调用付费模型。下一步由产品负责人审阅语言和内容方向，再决定修订或制作检索测试数据。
- 2026-09-01 用户确认首批 5 张内容方向后，已完成本地机器可读与检索验证。`backend/scripts/prepare_original_knowledge.py` 将样稿编译为 `data/knowledge/自建知识库/processed/cards.jsonl`，共 5 条；`backend/app/services/original_knowledge.py` 只用本地短语规则检索，无云端调用。20 组人工合成用例全部通过，覆盖单卡、多卡、无匹配兜底和“水果/海外”单字误命中反例，报告位于 `data/knowledge/自建知识库/evaluation/local-retrieval-report.json`。本轮模型调用与费用均为 0。20/20 不代表完整 RAG 或真实用户成功率；5 条记录仍为 `retrieval_enabled=false`、`production_eligible=false`，正式审校、权利签字和生产白名单仍为空。下一步建议先以影子模式接入后端，再根据真实命中记录决定是否扩大到 50 张。
- 2026-09-01 已将 5 张自建卡接入后端影子模式。确认梦象进入 `ready_to_generate` 后，本地检索只读取确认后的结构化梦象与澄清答案；SQLite 架构升至版本 3，新增 `original_knowledge_shadow_runs`，仅记录会话/修订、兜底状态、卡片编号、分数和匹配词，不复制梦境原文或检索查询。结果不加入 API 业务响应、不参与正式生成，异常时自动放行，删除会话时同步删除。新增配置检查接口和无用户文字的聚合报告脚本。完整后端 78 项测试通过；8001 已重启并报告 `mode=local`、`status=ready`、`card_count=5`、`uses_cloud_model=false`、`production_eligible=false`。初始真实影子记录为 0，本轮没有模型调用或费用。
- 2026-09-02 已按用户确认修正影子统计保留逻辑。SQLite 架构升至版本 4：`original_knowledge_shadow_runs` 仍只在会话存续时保存可关联的运行结果，删除会话时随之删除；新增 `original_knowledge_shadow_metrics` 只保留随机统计编号、兜底状态、命中卡片、分数、匹配词和时间，不含会话编号、修订号、梦境原文或结构化查询，因此删除梦境不会抹掉累计命中率。启动时会一次性将旧运行记录迁移为匿名事件，随后不重复迁移。针对删除后统计仍可用的测试、完整后端回归共 78 项通过；8001 已重启并完成迁移。本次迁移保留 1 条匿名兜底事件，无模型调用或费用。
- 2026-09-02 用户确认进入第二批知识卡制作。已确定 10 个高频选题和检索验收样例，见 `data/knowledge/自建知识库/第二批10张知识卡计划.md`：坠落、飞行、迟到与赶不上、无法移动、无法说话或呼救、学校与考试、家与旧房子、公共交通、陌生人、门与入口。当前只完成范围定义，未写正文、未调整生产开关、未调用模型。下一步为逐张核验可支持的依据并制作草稿。
- 2026-09-02 第二批 10 张已完成首轮依据核验与原创草稿，依据清单见 `data/knowledge/自建知识库/第二批10张知识卡依据清单.md`，草稿位于 `data/knowledge/自建知识库/样稿/`。其中坠落、飞行、迟到与赶不上、学校与考试只将公开研究用于主题或叙事分类。无法移动、无法说话或呼救只将睡眠瘫痪文献用于区分睡眠交界体验与现实求助边界。家与旧房子、陌生人、门与入口保持 `classification_only`，不输出心理归因。15 张卡已离线编译，全部仍为 `draft`、`original_pending_review`、`retrieval_enabled=false`、`production_eligible=false`。检索验收扩至 30 组人工合成用例，30/30 通过；完整后端回归 78 项与前端构建通过，模型调用与费用均为 0。下一步建议先重启后端加载 15 张卡，继续影子观察真实命中与兜底，期间不改变用户输出。
- 2026-09-02 已进入第三批选题阶段。`data/knowledge/自建知识库/第三批10张知识卡计划.md` 固定 10 张补组合关系的卡片，分别为寻找、躲藏、重复同一过程、道路与走廊、高处、陌生城市与建筑、父母与家人、人群与被围观、老师上级与权威人物、丢失与损坏。当前只完成选题和合成验收样例，不包含卡片正文，不改变 15 张卡的生产开关或影子输出。下一步为依据核验与原创草稿。
- 2026-09-02 第三批已完成首轮依据核验，见 `data/knowledge/自建知识库/第三批10张知识卡依据清单.md`。公开资料只用于梦境内容分类、组合检索与场景不连续边界，不支持对人物、空间、物件或循环情节作固定心理归因。所有第三批卡片仍未写正文、未编译、未加载影子模式，模型调用为 0。下一步为原创草稿。
- 2026-09-02 第三批 10 张已完成原创草稿、本地编译与验收。当前共 25 张卡，第三批覆盖寻找、躲藏、重复同一过程、道路与走廊、高处、陌生城市与建筑、父母与家人、人群与被围观、老师上级与权威人物、丢失与损坏。扩容后，旧用例中的合理多线索匹配也同步纳入期望结果，并移除了“牙齿碎了”被泛化识别为“物品损坏”的错误词条。40 组人工合成用例 40/40 通过，完整后端回归 78 项与前端构建通过。全部卡仍为 `draft`、`original_pending_review`、`retrieval_enabled=false`、`production_eligible=false`，模型调用与费用均为 0。下一步为重启后端加载 25 张卡进入影子观察，且不改变用户输出。
- 2026-09-02 已完成第四批选题，见 `data/knowledge/自建知识库/第四批10张知识卡计划.md`。这一批暂定森林与荒野、医院与诊所、卫生间与私密空间、伴侣与前任、儿童与小时候的自己、动物角色、蛇、猫与狗、镜子与倒影、手机与通信。当前只确定组合和安全边界，未写正文、未加载影子模式。下一步为依据核验。
- 2026-09-02 第四批已完成首轮依据核验，见 `data/knowledge/自建知识库/第四批10张知识卡依据清单.md`。医院与诊所卡只使用公开研究建立真实住院或持续困扰时的现实支持边界，不用梦境诊断疾病。其余动物、私密、关系、儿童、镜像和通信主题均保持 `classification_only`，不输出固定象征。当前未写正文、未编译、未加载影子模式，模型调用为 0。下一步为原创草稿。
- 2026-09-02 第四、五、六批已完成原创草稿、本地编译与验收，首批 50 张目录全部齐备。新增覆盖鱼与水生动物、昆虫与蜘蛛、钥匙与锁、车辆、火、风暴、死亡与葬礼、裸露与被看见、冲突与攻击、灾害、怀孕生产与婴儿、婚礼与仪式、重逢与告别、身体变化和超现实变化。`processed/cards.jsonl` 共 50 条，全部保持 `draft`、`original_pending_review`、`retrieval_enabled=false` 与 `production_eligible=false`。65 组合成检索用例 65/65 通过，完整后端回归 81 项和前端构建通过，模型调用与费用均为 0。当前运行的后端未因本轮重启，网页仍维持先前加载的卡片快照；新卡在完成审校、权利签字和显式加载决策前不得作为生产解读依据。下一步为以真实用户表达做影子命中观察，再按未覆盖模式决定第七批。
- 2026-09-02 已根据目录缺口与匿名影子统计确定第七批 25 张选题，见 `data/knowledge/自建知识库/第七批25张知识卡计划.md`。本批覆盖空间设施、日常场景、物件任务、身体体验、非现实角色、动物与关系变化，预留 DREAM-051 至 DREAM-075。匿名统计只用于衡量是否命中，不能反推或展示用户梦境原文。第一组 DREAM-051 至 DREAM-057 的依据与禁止断言已整理至 `第七批第一组7张知识卡依据清单.md`，尚未写草稿、编译或加载。所有新增卡保持生产开关关闭，云端模型调用为 0。下一步为撰写这一组的原创草稿与合成检索用例。
- 2026-09-02 第七批第一组 7 张已完成原创草稿、本地编译与验收，覆盖楼梯与电梯、桥边缘与跨越、黑暗停电与看不清、房间被侵入或无法离开、厨房餐桌与食物、商场超市与购物、工作场所与任务。`processed/cards.jsonl` 现有 57 条，79 条合成检索用例 79/79 通过，完整后端回归 82 项与前端构建通过。8001 已重启，影子配置报告 `card_count=57`。新增卡及此前卡片仍全部保持 `draft`、`original_pending_review`、`retrieval_enabled=false` 与 `production_eligible=false`，只用于本地匿名影子观察，未调用云端模型。下一步可继续制作第七批第二组 DREAM-058 至 DREAM-062。
- 2026-09-02 第七批第二组 5 张已完成依据、原创草稿、本地编译与验收，覆盖钱包金钱与支付、衣服鞋子与行李、照片信件与旧物、书写阅读与忘记内容、游戏表演与比赛。依据清单位于 `第七批第二组5张知识卡依据清单.md`。`processed/cards.jsonl` 现有 62 条，89 条合成检索用例 89/89 通过，完整后端回归 82 项与前端构建通过。8001 已重启，影子配置报告 `card_count=62`。全部新增卡仍为草稿观察材料，生产开关保持关闭，未调用云端模型。下一步可继续制作第七批第三组 DREAM-063 至 DREAM-068。
- 2026-09-02 第七批第三组 6 张已完成依据、原创草稿、本地编译与验收，覆盖被困逃不出与失去方向、迟缓失控与刹不住、溺水下沉与无法呼吸、血受伤与疼痛、头发外貌与脸部变化、生病检查与药物。依据清单位于 `第七批第三组6张知识卡依据清单.md`。涉及身体与安全的卡片只记录画面与感受，明确排除医学诊断、风险预言和治疗建议；现实急症转为现实支持边界。`processed/cards.jsonl` 现有 68 条，101 条合成检索用例 101/101 通过，完整后端回归 82 项与前端构建通过。8001 已重启，影子配置报告 `card_count=68`。所有卡继续只用于本地匿名影子观察，未调用云端模型。下一步可继续制作第七批第四组 DREAM-069 至 DREAM-075。
- 2026-09-02 第七批第四组 7 张已完成依据、原创草稿、本地编译与验收，覆盖鬼怪怪物与非人追随者、鸟与飞行动物、马狼与大型动物、宠物走失与动物受伤、背叛隐瞒与关系误会、搬家离开与旅行准备、时间失序与年龄变化。依据清单位于 `第七批第四组7张知识卡依据清单.md`。第七批 25 张选题已全部制作完成，`processed/cards.jsonl` 现有 75 条，115 条合成检索用例 115/115 通过，完整后端回归 82 项与前端构建通过。8001 已重启，影子配置报告 `card_count=75`。全部卡仍为 `draft`、`original_pending_review`、`retrieval_enabled=false` 与 `production_eligible=false`，只用于本地匿名影子观察，未调用云端模型。下一步应重新收集一批真实确认后的匿名命中指标，再决定是否补第八批或转入结果卡图片与下载闭环。

### 2026-08-31 输入区局部视觉更新

- 2026-09-01 语音控件更新：仅将“语音记录”按钮替换为用户提供的 VoiceInput 视觉，新增 `src/components/ui/voice-input.tsx/.css`，使用 motion/react 和已有 lucide-react。28px高麦克风控件，收听时展开旋转停止方块、12条装饰音条及计时；音条不是实际音量测量。状态受现有 speechStatus 控制，实际 listening 后才计时；连接/收尾显示明确状态，停止及错误退回原按钮。保留语音引擎和所有其他UI。固定音条高度、使用scaleY；后台/离屏暂停持续动画与计时间隔，恢复时按实际经过时间更新，卸载清理监听和定时器，支持减少动态效果偏好。构建、类型检查和8项语音单测通过，模拟浏览器验证收听/计时/停止/网络错误；空输入框保持44px高，未使用真实麦克风。

- 2026-09-01 最新决定：用户要求完整移除刚添加的标题星光特效。已删除 SparklesText 的页面引用及两个本地组件/样式文件，恢复原始标题结构与样式，移除专用 framer-motion 依赖。其他文字、布局、极光背景、按钮、输入与语音功能不变。下方标题星光试用记录仅为历史，不应重新启用。

- 2026-09-01 标题星光：用户确认仅给“将梦境 / 留作一张卡”加 SparklesText 特效，不改文字、字体、字号、颜色、位置和两行排版。新增 `src/components/ui/sparkles-text.tsx/.css` 和 framer-motion 依赖，保留示例紫粉双色、10颗SVG星光及淡入/缩放/旋转效果。不采用示例100ms轮询，仅星光寿命到期时更新位置；离屏、页面隐藏及减少动态效果偏好下停止动画，卸载清理监听与计时器。输入框、FlowButton、极光及后端不变。构建、TypeScript检查、8项语音测试通过；浏览器核对标题两行字体颜色、10颗装饰星光、输入操作，无控制台错误/警告。已关闭临时测试标签。先前删除的玻璃流光不应恢复。

- 2026-09-01 最新决定：用户明确弃用刚加入的浅色玻璃输入框与双层白色流光。已删除 `glassmorphism-input.tsx/.css`、页面导入/挂载及glass-composer类名，恢复原有dream-composer样式，重建生产文件清除相关代码。本次无新增依赖或落盘截图需要清理。极光背景、Flow Button、语音和提示逻辑保留。以下玻璃流光试用记录仅为历史，不应重新启用。
- 2026-09-01 最新输入框视觉试用：用户澄清将所给Glassmorphism CTA的材质/动效用于整个输入框，不改右侧Flow Button、不加头像/魔法棒。新增 `glassmorphism-input.tsx/.css`，输入框增加glass-composer样式。按浅色UI适配：浅色半透明玻璃、8px背景模糊、白色双旋转流光（4秒）及淡紫投影；未套用深色CTA底色或整框hover放大。内容、语音、字数和布局保留，无新增依赖；之前删除的border-beam没有恢复。
- 新流光仅装饰层，pointer-events:none；沿1px边缘裁切，不遮挡文字。visibilitychange和IntersectionObserver控制后台/离屏暂停，监听卸载清理；减少动态效果时禁用动画。动画仅transform，但仍是持续视觉效果，未做CPU/温度实测，不承诺零负担。
- 验证：560×44空输入，长文560×200且右侧按钮中心对齐；390px窄屏DOM无横向溢出（截图工具缩放异常，不宣称窄屏像素级验收）；模拟语音开启/停止、提交禁用正常，无真实麦克风或云端调用；构建/类型检查/8项语音单测通过，浏览器无error/warn。临时测试页已关闭。
- 2026-09-01 最新决定：用户认为边缘流光/大范围泛光不适合浅色UI，明确要求全部删除。已移除页面引用、`border-beam-search.tsx`、`composer-border-beam.tsx`、`composer-border-beam.css`及测试截图`work/beam-desktop.png`，卸载border-beam并同步锁文件，重建生产产物以清除旧流光代码。极光背景、Flow Button、输入/语音/提示逻辑保留。下方流光记录仅为历史，不应重新启用。
- 2026-09-01 流光最新要求：用户明确要求恢复向上扩散及大范围光圈；移除::before与bloom的隐藏规则、取消仅下半边裁切，恢复line组件原生泛光。保留满强度细边、3.1秒、staticColors；暂停和减少动态效果规则扩展到全部泛光层。输入栏尺寸不变。渲染成本增加，未进行CPU基准测试。
- 2026-09-01 流光强度最新确认：用户要求“直接拉满”，strength由.65改为1，CSS `--beam-stroke-opacity:6.25`抵消light/line预设.16衰减，边缘层峰值不透明度达到1（仍保留周期首尾淡入淡出及渐变自身透明区域）。继续只保留1px底边，无内泛光/外围光晕，3.1秒周期与暂停逻辑不变。
- 2026-09-01 输入栏底边流光：接入 `border-beam@1.3.0`，导出位于 `src/components/ui/border-beam-search.tsx`；业务装饰组件 `composer-border-beam.tsx` 和同名CSS独立封装。使用line/light/colorful、3.1秒、strength .65、staticColors（保留彩色但不循环色相滤镜）；CSS隐藏::before和bloom，限定::after下半边，只保留边框细线。绝对装饰层不包装真实输入，pointer-events:none，不变更原背景/阴影/语音/位置/尺寸。
- 接入document.visibilitychange控制暂停，卸载清理；减少动态效果偏好下关闭装饰层及其动画，保留原输入边框。依赖自带离屏暂停。本次内置浏览器的跨页检查仍报告document.hidden=false，未模拟隐藏事件，不能宣称后台暂停已完成浏览器实测；实际发热/CPU开关对比也未做。
- 已检查桌面560×44、长文560×200时装饰层同步增高且按钮居中；390px窄屏输入358px无溢出；模拟语音可开启/停止、提交禁用状态保留，无真实录音。构建、类型检查、8项语音单测通过；无新增开发服务器，测试页已关闭。视觉为低强度底边流光，待用户确认可见度。
- 2026-09-01 提示外观微调：空输入提示去掉末尾句号、文字居中，上下padding由15px改为10px（实际高度约45px），背景透明度由.96降至.72，边框及阴影减淡；位置和3秒/操作关闭逻辑不变。浏览器视觉与计算样式检查、构建及类型检查通过。
- 2026-09-01：空输入点击“进入梦境”的提示已独立为 `inputNotice`，显示在示例标签下方26px、水平居中；绝对定位不挤动布局，显示时隐藏原装饰竖线但保留其占位。约3秒自动关闭，后续pointerdown/keydown/input/wheel/touchmove/scroll立即关闭；不监听开启提示的click，重复触发会重建计时器，卸载时清理计时和监听。其他网络/修订错误仍使用原toast，语音反馈不改。浏览器验证显示、自动消失（点击返回后约2825ms）、点击输入框立即关闭；构建/类型检查通过，浏览器无error。
- Flow Button尺寸后续微调：用户要求视觉缩小且与输入栏等高。实测原本两者均44px，因此保留按钮44px高度，宽112→100.8px；文字/箭头/内部间距及位移统一乘0.9。默认边框改为与空输入栏一致的1px rgba(17,17,17,.11)。垂直中心一致，原动效保留。
- 后续用户确认替换玻璃按钮：现使用 `src/components/ui/flow-button.tsx`，配套 `flow-button.css`。默认透明胶囊、右箭头；悬停/键盘焦点时黑圆扩散、文字变白右移、左箭头进入、圆角12px；点击轻微缩放。文案不做双段切换，真实加载期间沿用“正在整理”。
- 已安装 `lucide-react`。组件目录沿用 `src/components/ui`，`@/components/ui` 别名已有配置；全局样式仍为 `src/index.css`。无需初始化shadcn CLI或添加演示页面，也没有更改项目框架。
- 基于用户组件做集成适配：原生button属性透传、disabled与aria-busy、focus-visible、减少动态效果；扩散改用固定220px层缩放、箭头用transform，未引入GSAP或持续渲染循环。旧玻璃样式已移除，以下玻璃描述仅为此前历史。
- 按用户确认，桌面输入框宽560px不变，空白高度86px压至44px；保留随文字增长，textarea最大184px。
- “进入梦境”改为静态深色玻璃胶囊（白色高光边缘/无衬线600字重），移到输入框外右侧16px并垂直居中。“语音记录”放框内右端，字数在其左侧。
- ≤840px时按钮折行到框外右下，优先保持输入框宽度、防止横向溢出。
- 没有增加持续动画/Canvas/WebGL/依赖；极光、语音状态、7秒/90ms提示轮换均保留。构建、类型检查、8项语音单测及桌面/窄屏局部检查通过，待用户确认视觉。

产品经理完成第一阶段验收清单后，先记录验收结论。随后再单独确认是否启动第二阶段的云端内容安全审核；RAG 仍需等待明确指令和合法、可追溯的书籍资料。
