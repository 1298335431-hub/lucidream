# V1 本地验收记录

日期：2026-09-14。范围：当前本地项目与 http://127.0.0.1:8443/。
本轮只测试和检查，未调用真实生图，未消耗邀请码额度，未修改业务代码或覆盖真实数据库。

## 已完成

- 后端自动测试：173 项通过。包含邀请码认证与失效、跨账号资源访问隔离、并发额度预占、阻止第 6 次任务、同任务重试不重复计费、质量提示不自动重绘、一次补生成限制、图片版本持久化与隔离、提交超时防重复提交。
- 前端自动测试：14 项通过，覆盖结果页视图存取与语音状态逻辑。视图单元测试不等同完整页面恢复测试。
- TypeScript 检查、生产构建通过；构建仍有约 600 KB JS 分包警告，不是构建失败。
- 本地健康接口 HTTP 200。
- 独立 Chromium 上操作真实前端、模拟认证接口：无效码提示、登录保留输入草稿、刷新保留草稿、退出清空草稿、再次登录进入空白输入页均通过。不使用用户现有浏览器会话。
- 输入页在 375、390、768、1440 像素视口下未检测到页面横向溢出；不代表所有页面或手机实机已验收。
- 当前 SQLite 数据库通过在线备份 API 复制到临时目录，再从备份恢复另一份数据库，重新初始化 Repository 后 14 张表的行数与内容摘要完全一致，integrity_check 为 ok。含梦境、解读、图片资产、邀请码、额度和版本选择表。未停止或覆盖正在使用的服务。
- 恢复演练目录：/var/folders/p6/7pb9vxm57xq8kys3j17nl2yc0000gn/T/dreamcard-acceptance-n2s9_ucu 。这是含私密数据的临时验收副本，不是长期备份，不应对外分享。

## 已复现或确认的遗漏

1. 刷新恢复遇到临时 503 会丢失前端当前记录入口。独立浏览器先保存 recentSession，模拟 auth/me 成功、读取梦境返回 503，页面返回输入页且 recentSession 变为 null。数据库数据不受此操作影响。src/App.tsx 的 restoreSession 外层 catch 对非中止错误一律删除会话入口，应区分临时故障与真正无权访问/不存在。
2. 梦境册仍用 src/components/product-modules.tsx 的 DREAMS 常量，未展示真实账户历史；详情下载仍是演示提示。后端已保存数据与提供列表，并不等于前端历史访问已接好。
3. 隐私设置里的保存开关、自动保留期限、导出、删除仍未对接真实后端行为，不能作为已生效的数据承诺上线。
4. 浏览器首页仍展示免费永久 3 次、Plus 每月 15 次、Pro 每月 30 次和 4K 等套餐文案，与本次邀请码每码 5 次规则不一致。

## 尚未验收

- 正式域名 HTTPS、生产 Cookie/跨域和线上环境配置。
- 手机实机全流程、移动 Safari/微信等实际下载兼容性。
- 本轮未在浏览器完整复测梦卡日期、标识、边框、版本切换后的 PNG 导出；用户已提供过下载样例，但不等于这些组合全部通过。
- 真正供应商任务在服务重启/断网下的完整恢复未进行故障注入；当前主要是模拟接口自动测试。
- 数据库恢复演练已完成，但生产部署的持久卷、定时备份、保留策略和整套服务灾备恢复仍待验证。

## 建议结论

不能将当前结果标记为全部验收通过。优先修复临时故障恢复入口问题；接好真实梦境册或暂时隐藏演示入口；移除未生效隐私承诺；统一邀请码额度文案。再进行线上与移动端验收。

## 第一项修复复验

随后按用户授权仅修复刷新恢复问题：认证、梦境、文章、图片恢复遇到网络/服务故障时保留记录入口和结果页偏好，显示重新连接按钮；按钮重新加载后只查询原记录，不提交生图。恢复请求设置 20 秒超时，不会永久停在等待状态。记录明确返回 403/404 时才清理不可访问入口；认证失效仍走原登录流程。

新增 tests/browser-restore.mjs，独立浏览器拦截接口，7 项全部通过：认证/梦境/文章/图片临时 503、网络断开后重连返回梦卡页，以及记录明确 403/404 的清理行为。重连测试同时确认没有 POST 请求。原有 14 项前端测试通过，类型检查和构建通过。其余验收问题仍未修改。

## 第二、三项修复复验

- 梦境册改用当前账户真实列表。后端仅查询所属账户、当前内容版本，返回标题、日期、原文、标签和生成状态；支持搜索和分页。界面包含加载、空列表、失败重试和加载更多。
- 查看解读和梦卡复用已有恢复流程；未完成/失败记录明确打开已保存梦象，不强行读取失败的解读。开始新记录只清理本机入口，不删除历史数据库记录。已有图片的下载沿用梦卡工作台下载功能。
- 隐私页面移除未接后端的保存/个性化/匿名开关、自动保留期限和演示导出/删除操作。说明服务端保存、浏览器草稿与显示设置、AI 服务处理、暂无定期清理/批量管理，以及邀请码共享的访问风险。不承诺尚未实现的控制能力。
- 新增后端账户列表测试：私有列表、分页、搜索、图片资产存在判断、原文/标题回退、旧版本结果隔离、参数校验、不扣额度。后端总计 175 项通过。
- 新增 tests/browser-archive.mjs：独立 Chromium 模拟接口验证列表失败重试、加载更多、空搜索、打开保存文章/梦卡、打开未完成记录、隐私无无效控件、390px 无横向溢出、新记录不删除旧记录。过程中没有生成或删除请求。该测试与 7 项恢复浏览器测试通过；15 项前端单元测试通过；类型检查和构建通过。
- 本地后端在确认没有处理中的生成任务后重启，新列表接口已加载，健康接口正常。未调用真实生图，未扣额度。
- 第四项首页旧套餐文案仍未修改；正式 HTTPS、手机实机下载及生产备份策略仍待验收。
