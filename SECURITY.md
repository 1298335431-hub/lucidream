# 安全说明

## 不应提交的内容

- `.env`、云平台凭据、私钥、OAuth app_key、Access Secret 与用户令牌
- 真实体验码、用户梦境数据库、会话、图片备份与含敏感内容的日志
- 包含上述内容的截图、压缩包或部署缓存

`.gitignore` 只是第一层保护；推送前仍需检查实际暂存文件。若密钥曾经泄露，删除文件不足以恢复安全，应先在服务方轮换密钥，再处理历史记录。

## 运行边界

OAuth Node 服务仅供后端在回环地址访问，不能把其 `/api/login/*` 和示例接口直接暴露公网。授权回调查询串、请求 Cookie 和响应令牌不能记录到代理或应用日志中。

正式环境启用认证、HTTPS 与安全 Cookie。临时演示不提供可靠持久化，不适合存放敏感梦境或重要个人资料。共享体验码意味着共享同一账号，不适合多人独立隐私测试。

## 报告问题

请通过仓库维护者的私密联系方式反馈漏洞，不要在公开 Issue 中提交可用凭据、用户数据或未经脱敏的授权链接。报告应说明受影响版本、复现条件和预期行为。
